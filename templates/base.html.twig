<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Gestion des événements sportifs{% endblock %}</title>
    <link rel="stylesheet" href="{{ asset('css/styles.css') }}">

    <!-- Ajouter le CDN de Leaflet pour la carte -->
    <link rel="stylesheet" href="https://unpkg.com/leaflet@1.7.1/dist/leaflet.css" />
    <script src="https://unpkg.com/leaflet@1.7.1/dist/leaflet.js"></script>
</head>
<body>
    <header>
        <nav>
            <ul>
                <li><a href="{{ path('home') }}">Accueil</a></li>
                <li><a href="{{ path('list_events') }}">Liste des événements</a></li>
                <li><a href="{{ path('add_event') }}">Créer un événement</a></li>
            </ul>
        </nav>
    </header>


    <main>
        {% for message in app.flashes('success') %}
            <div class="alert alert-success">{{ message }}</div>
        {% endfor %}
        {% for message in app.flashes('error') %}
            <div class="alert alert-danger">{{ message }}</div>
        {% endfor %}

        {% block body %}{% endblock %}
    </main>
    <footer>
        <p>&copy; 2024 Gestion des événements sportifs</p>
    </footer>
</body>
</html>
